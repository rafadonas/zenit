# ZENIT mobile

Reserved for the offline-first Flutter Android application. Flutter tooling is
not available on the current development machine, so scaffolding is deferred
until the toolchain can be validated.
